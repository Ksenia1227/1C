{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "frontend": "npm run serve --prefix frontend",
    "backend": "npm run serve-dev --prefix backend",
    "dev": "concurrently \"npm:frontend\" \"npm:backend\""
  }
}
